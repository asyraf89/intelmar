# My first workflow

name: workflow1
on:
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step1 for testing
        run: echo "hello from Asyraf"
      - name: step2
        run: cat /etc/os-release
## Name is not important in a step
      - run: uname -a

  
