# My first workflow

name: workflow1
run-name: Ubuntu20.04 - ${{ github.event.pull_request.title }}
on:
  push:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step1 for testing
        run: |
          echo "hello from Asyraf"
          sleep 30
      - name: step2
        run: |
          cat /etc/os-release
          sleep 5
## Name is not important in a step
      - run: uname -a

  
